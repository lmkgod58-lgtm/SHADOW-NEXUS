services:
  - type: web
    name: shadow-nexus-gateway
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --threads 2 --timeout 60
    healthCheckPath: /health
    autoDeploy: true
